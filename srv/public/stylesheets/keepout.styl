base-color = #9c27b0
accent-color = #ffe57f

#container
    width 900px
    margin 10% auto

#header
    background-color base-color
    width 100%
    height 50px
    margin 0
    padding 0
    float left

board-length = 480px
board-min-length = 320px
square-length = 60px
square-min-length = 40px

#board
    width board-length
    min-width board-min-length
    height board-length
    min-height board-min-length
    float left
    border 1px solid #000

.square
    width square-length
    min-width square-min-length
    height square-length
    min-width square-min-length
    background-color square-color
    
chat-width = 400px
chat-min-width = 280px

#chatbox
    width chat-width
    min-width chat-min-width
    height board-length
    min-height board-min-length
    float right
    border 1px solid #000

#messages
    width 90%
    margin 5%

#input
    width 90%
    height 36px
    line-height 36px

#inputBox
    height 30px

#actionButton
    float right