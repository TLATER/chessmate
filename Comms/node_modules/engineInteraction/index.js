var Bus = require('../systemBus');

function engineChannel(stringDesiredMove){
    Bus.on('newMove', function(move) {
        Bus.emit('sendMove', move);
    });
}

var channel = new engineChannel();

module.exports = channel;

// Taking client request and send it to game engine in correct form