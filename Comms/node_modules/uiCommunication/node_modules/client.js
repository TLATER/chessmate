function client(response) {
    //var response = response;
    
    this.getResponse = function() {
        return response;
    };
}

client.prototype.sendResponse = function(data){
    this.getResponse().send(data);
    // code to send a response
};

module.exports = client;