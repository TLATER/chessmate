/*Converting user input into an array to send to game engine
  by spliting at "-"
*/
var Bus = require('../systemBus');

function engineChannel(stringDesiredMove){
    Bus.on('newMove', function(move) {

    });
}

function stringToJSON(move) {
  var moveArray = move.split("-");
  var moveJSON = { "currentPos" : moveArray[0], "desiredPos" : moveArray[1] };
  return moveJSON;
}

module.exports = stringToJSON;