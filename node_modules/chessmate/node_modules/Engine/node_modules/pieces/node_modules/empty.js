var Piece = require('./Piece');

// Now, this one will call the "superclass"'s constructor and apply it to itself
function empty(color, id) {
    Piece.call(this);
    this.name = ' ';
    this.color = -1;
    this.id = id;
}

empty.prototype = Object.create(Piece.prototype);

module.exports = empty;