var Piece = require('./Piece');

// Now, this one will call the "superclass"'s constructor and apply it to itself
function knight(color, id, square) {
    Piece.call(this);
    this.name = 'S';
    this.color = color;
    this.id = id;

    this.hack = square;
}

knight.prototype = Object.create(Piece.prototype);

module.exports = knight;