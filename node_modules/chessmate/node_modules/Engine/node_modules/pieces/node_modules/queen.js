var Piece = require('./Piece');

// Now, this one will call the "superclass"'s constructor and apply it to itself
function queen(color, id, square) {
    Piece.call(this);
    this.name = 'Q';
    this.color = color;
    this.id = id;

    this.hack = square;
}

queen.prototype = Object.create(Piece.prototype);

module.exports = queen;