//var checkCheck = require('checkCheck');
var isMoveLegal = require('legalChecker');

function observer() {
    var isCheck = false;

    this.getCheck = function() {
        return isCheck;
    };

}

observer.prototype.move = function(piece, file, rank) {
/*  Check implemented later
    if (this.getCheck())
        checkCheck();
*/

    // Translate move to amount moved

    // Check if the move is legal for the given piece
    return isMoveLegal(piece, file, rank);
};

module.exports = observer;