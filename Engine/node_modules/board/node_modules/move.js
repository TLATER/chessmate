var square = require('./square');

function move(piece, moveRank, moveFile) {
    switch (piece.name) {
        case 'R':

            break;
        case 'K':

            break;
        case 'Q':

            break;
        case 'S':

            break;
        case 'P':

            break;
        case 'B':

            break;
    }
}

function checkRookSpaces(moveRank, moveFile) {
    // If one of the ranks is occupied, return false
    for (var i; i < moveRank; i++) {
        if (square.getPiecemap() !== ' ')
            return false;
    }
    // If one of the files is occupied, return false
    for (var i; i < moveFile; i++) {
        if (square.getPiecemap() !== ' ')
            return false
    }
}